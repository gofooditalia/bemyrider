plugins {
    id 'com.android.application'
    id 'com.google.gms.google-services'
}

// Helper to read local.properties
def getLocalProperty(String key) {
    Properties properties = new Properties()
    if (rootProject.file('local.properties').exists()) {
        properties.load(rootProject.file('local.properties').newDataInputStream())
    }
    return properties.getProperty(key)
}

android {
    namespace "com.app.bemyrider"
    compileSdk 35

    defaultConfig {
        applicationId "com.app.bemyrider"
        minSdk 29
        targetSdk 35
        versionCode 13
        versionName "1.1.2"
        multiDexEnabled true
        
        // Inject API Key into Manifest
        manifestPlaceholders = [googleMapsApiKey: getLocalProperty('GOOGLE_MAPS_API_KEY') ?: ""]
    }

    signingConfigs {
        release {
            keyAlias 'bemyrider'
            keyPassword '123456'
            storePassword '123456'
            storeFile file('bemyrider.jks')
        }
    }
    
    buildTypes {
        release {
            minifyEnabled true
            proguardFiles getDefaultProguardFile('proguard-android-optimize.txt'), 'proguard-rules.pro'
            signingConfig signingConfigs.release
        }
        debug {
            minifyEnabled false
        }
    }

    compileOptions {
        sourceCompatibility JavaVersion.VERSION_1_8
        targetCompatibility JavaVersion.VERSION_1_8
    }

    dataBinding {
        enabled = true
    }

    lintOptions {
        checkReleaseBuilds false
        // Or, if you prefer, you can continue to check for errors in release builds,
        // but continue the build even when errors are found:
        abortOnError false
    }

    buildFeatures {
        viewBinding true
    }

}

dependencies {
    implementation 'com.google.android.material:material:1.13.0'
    implementation 'com.google.code.gson:gson:2.10.1'
    // implementation 'com.facebook.android:facebook-android-sdk:14.1.0' // REMOVED
    implementation 'com.google.android.gms:play-services-auth:21.4.0'
    implementation 'com.google.firebase:firebase-messaging:24.1.0' // Updated to latest stable compatible
    implementation 'com.intuit.sdp:sdp-android:1.1.0' // Updated
    implementation 'com.intuit.ssp:ssp-android:1.1.0' // Updated
    implementation 'com.squareup.picasso:picasso:2.71828'
    implementation 'com.google.android.libraries.places:places:3.4.0'
    implementation 'com.google.android.gms:play-services-location:21.2.0' // Updated
    
    implementation 'com.stripe:stripe-android:20.48.1' // Updated to latest stable v20.x
    implementation 'com.github.yalantis:ucrop:2.2.6'
    
    implementation 'androidx.constraintlayout:constraintlayout:2.1.4'
    implementation 'org.greenrobot:eventbus:3.3.1'
    implementation 'com.wdullaer:materialdatetimepicker:4.2.3'
    implementation 'androidx.multidex:multidex:2.0.1'
    implementation 'androidx.appcompat:appcompat:1.7.1'
    implementation 'androidx.swiperefreshlayout:swiperefreshlayout:1.1.0'
    implementation 'androidx.localbroadcastmanager:localbroadcastmanager:1.1.0'

    /** Signature view*/
    implementation 'com.github.gcacace:signature-pad:1.3.1'
    implementation 'com.google.firebase:protolite-well-known-types:18.0.0'
    implementation "androidx.security:security-crypto:1.1.0-alpha06"

    //implementation 'androidx.work:work-runtime:2.7.1'
}
